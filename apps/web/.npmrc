# Pin this project to the PUBLIC npm registry so CI and the Docker build (which have no
# corporate Artifactory credentials) resolve the same packages the lockfile records.
# No credentials here — do not add any. Your ~/.npmrc (Artifactory + auth) is untouched
# and stays out of the repo.
registry=https://registry.npmjs.org/
