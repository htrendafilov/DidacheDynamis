name: ci

on:
  pull_request:
  push:
    branches: [main]

jobs:
  check:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: "3.13"

      - uses: actions/setup-node@v4
        with:
          node-version: "22"

      # Backend tooling (present once apps/api exists)
      - name: Install backend tooling
        run: pipx install ruff pytest || pip install ruff pytest
        continue-on-error: true

      - name: Run checks
        run: bash scripts/check.sh
