name: deploy

# Manual-only until M1: the image bakes in data/content.sqlite (built by the importer)
# and the deploy step needs VM_HOST/VM_USER/SSH_DEPLOY_KEY/DEPLOY_DIR secrets.
# Flip to `push: branches: [main]` once content.sqlite exists and secrets are set.
on:
  workflow_dispatch:

concurrency:
  group: deploy-production
  cancel-in-progress: false

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write
    env:
      IMAGE: ghcr.io/${{ github.repository }}
    steps:
      - uses: actions/checkout@v4
        with:
          lfs: true

      - name: Log in to GHCR
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Build and push image
        uses: docker/build-push-action@v6
        with:
          context: .
          file: deploy/Dockerfile
          push: true
          build-args: |
            VITE_DROPBOX_APP_KEY=${{ vars.DROPBOX_APP_KEY }}
          tags: |
            ${{ env.IMAGE }}:${{ github.sha }}
            ${{ env.IMAGE }}:latest

      # Deploy to the VM over SSH. The VM only pulls from GHCR.
      - name: Deploy to VM
        uses: appleboy/ssh-action@v1
        with:
          host: ${{ secrets.VM_HOST }}
          username: ${{ secrets.VM_USER }}
          key: ${{ secrets.SSH_DEPLOY_KEY }}
          script: |
            cd ${{ secrets.DEPLOY_DIR }}
            export IMAGE_TAG=${{ github.sha }}
            docker compose pull
            docker compose up -d
            # smoke check
            for i in $(seq 1 15); do
              if curl -fsS http://127.0.0.1:${API_PORT:-8080}/health; then echo "healthy"; exit 0; fi
              sleep 2
            done
            echo "health check failed"; exit 1
